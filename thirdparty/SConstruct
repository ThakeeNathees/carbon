#!python
import os
Import('env')

SOURCES = []

## source files
if env['platform'] == "osx":
    pass
elif env['platform'] in ('x11', 'linux'):
    pass
elif env['platform'] == "windows":
    SOURCES += [
        File('dlfcn-win32/dlfcn.c')
    ]
    pass

env.LIBS['cb_core'] += SOURCES